import {useMemo,useRef,useState} from 'react';
import {Box,Layers3,SlidersHorizontal,Download,ChevronRight,RotateCcw,Maximize,Settings2,Eye,TriangleAlert,PanelLeft,Upload,ArrowUpRight,BatteryCharging,Network,X,Info} from 'lucide-react';
import {buildModel} from './domain/model';
import {useStudio} from './state/store';
import {Viewer,type ViewerHandle} from './scene/Viewer';
import {qualification,type Visibility} from './config/schema';
import './style.css';
import {ConfigPanel,InspectPanel,ExportPanel} from './components/Panels';
export default function App({brandName='SOLARWORLD',brandLogo=null,projectName}:{brandName?:string;brandLogo?:string|null;projectName?:string}={}){const {config,selected,scope,update,focus,select,reset,message,notify}=useStudio(),model=useMemo(()=>buildModel(config),[config]),viewer=useRef<ViewerHandle>(null),[tab,setTab]=useState('inspect'),[panel,setPanel]=useState('layers');const s=model.stats,node=model.nodes.find(n=>n.id===selected),errors=model.warnings.filter(w=>w.level==='error');
 return <div className={`app ${config.presentation?'presentation':''}`}><header><div className="brand">{config.logo?<img src={config.logo} alt={brandName}/>:brandLogo?<img src={brandLogo} alt={brandName}/>:<strong>{brandName}</strong>}<i/><div>BESS Design Studio<small>CONCEPT ENGINEERING / REV 01</small></div></div><div className="header-actions"><span className="local-tag">Local workspace</span><button className="plain" onClick={()=>update(c=>{c.presentation=!c.presentation;})}><Eye size={16}/>{config.presentation?'Engineering':'Present'}</button><button className="primary" onClick={()=>setTab('export')}><Download size={16}/> Export</button></div></header>
 <div className="workspace"><aside className="hierarchy"><div className="panel-heading"><span>PROJECT EXPLORER</span><PanelLeft size={15}/></div><div className="project-name"><div className="project-icon"><BatteryCharging size={23}/></div><div><b>{projectName??'5 MWh BESS'}</b><small>Custom concept enclosure</small></div></div><div className="left-tabs"><button className={panel==='layers'?'active':''} onClick={()=>setPanel('layers')}>Assembly</button><button className={panel==='visibility'?'active':''} onClick={()=>setPanel('visibility')}>Visibility</button></div>{panel==='layers'?<div className="tree"><button aria-label="Container assembly" className={selected==='BESS'?'selected':''} onClick={()=>focus('BESS')}><Box size={16}/><b>Container assembly</b><span>48</span></button>{[0,1].map(bank=><div key={bank}><h4>BANK {bank===0?'A':'B'} <span>{s.racks/2} racks</span></h4>{model.racks.filter(r=>r.bank===bank).map(r=><div key={r.id}><button className={selected===r.id?'selected':''} onClick={()=>{select(r.id);}} onDoubleClick={()=>focus(r.id)}><ChevronRight size={14}/><Layers3 size={15}/>{r.id}<span>{s.seriesPacks} packs</span></button>{belongsSelection(selected,r.id)&&model.packs.filter(p=>p.parent===r.id).map(p=><button key={p.id} className={`pack-item ${selected===p.id?'selected':''}`} onClick={()=>select(p.id)} onDoubleClick={()=>focus(p.id)}><Box size={14}/>{p.id.split('/')[1]}<span>104 cells</span></button>)}</div>)}</div>)}</div>:<div className="visibility-list">{Object.keys(config.visibility).map(key=><label key={key}><input type="checkbox" checked={config.visibility[key as Visibility]} onChange={e=>update(c=>{c.visibility[key as Visibility]=e.target.checked;})}/>{({roof:'Container roof',walls:'Side walls',lids:'Pack lids',hv:'HV conductors',bms:'BMS communications'} as Record<string,string>)[key]??key}</label>)}</div>}<div className="left-bottom"><span className="swatch"/>LFP · {s.equivalent}<small>104S1P per pack · SB332314</small><p>Double-click a component to isolate it.</p></div></aside>
 <main><div className="canvas-toolbar"><div className="breadcrumbs"><button onClick={()=>focus('BESS')}>BESS</button>{scope!=='BESS'&&scope.split('/').map((p,i)=><span key={i}><ChevronRight size={13}/><button onClick={()=>focus(scope.split('/').slice(0,i+1).join('/'))}>{p}</button></span>)}<span className="concept-chip">CONCEPT</span></div><div className="view-buttons">{(['iso','top','front','side'] as const).map(v=><button title={`${v} view`} className={config.camera.view===v?'active':''} key={v} onClick={()=>update(c=>{c.camera.view=v;})}>{v==='iso'?'Isometric':v}</button>)}<button title="Reset camera" onClick={()=>focus(scope)}><RotateCcw size={15}/></button></div></div><div className="canvas-wrap"><Viewer ref={viewer} model={model}/><div className="canvas-title"><span>ASSEMBLY VIEW</span><h1>{scope==='BESS'?'Containerized energy storage':scope.includes('/C')?'Prismatic LFP cell':scope.includes('/P')?'104-cell battery pack':'Battery rack'}</h1><p>{scope==='BESS'?`Two banks · ${s.racks} racks · ${s.packs} packs`:`Installed component ${scope}`}</p></div>{errors.length>0&&<button className="voltage-alert" onClick={()=>setTab('validation')}><TriangleAlert size={16}/><span>{errors[0].code==='overvoltage'?'Maximum voltage exceeds equipment limit':errors[0].text}<small>{s.maxVoltage.toLocaleString()} V max / {config.equipment.maxVoltage?.toLocaleString()??'unset'} V equipment</small></span><ChevronRight size={15}/></button>}{config.explode>0&&<div className="exploded-note">Conductors hidden during explosion · assembled routing retained</div>}<div className="canvas-bottom"><div className="axis"><b>Y</b><span>↗ X</span><i>Z ↘</i></div><div className="canvas-hint">Drag to orbit <i/> Scroll to zoom <i/> Right-drag to pan</div><button onClick={()=>focus(selected)} title="Fit to selection"><Maximize size={16}/></button></div></div><div className="scene-controls"><button className={!config.visibility.roof?'active':''} onClick={()=>update(c=>{c.visibility.roof=!c.visibility.roof;})}><Layers3 size={16}/> Roof off</button><button className={!config.visibility.walls?'active':''} onClick={()=>update(c=>{c.visibility.walls=!c.visibility.walls;})}>Cutaway</button><div className="divider"/><label>Explode <input aria-label="Exploded view" type="range" min="0" max="1" step=".05" value={config.explode} onChange={e=>update(c=>{c.explode=Number(e.target.value);})}/><span>{Math.round(config.explode*100)}%</span></label><button className={config.highlight?'active':''} onClick={()=>update(c=>{c.highlight=!c.highlight;c.visibility.busbars=true;})}><Network size={16}/>Electrical path</button></div><div className="stats">{[[s.energy.toLocaleString('en',{maximumFractionDigits:2}),'kWh','Nominal DC energy'],[(s.power/1000).toFixed(3),'MW','Nominal DC power'],[s.cells.toLocaleString(),'cells',`${s.packs} packs · ${s.racks} racks`],[s.voltage.toLocaleString(),'V','Nominal string voltage']].map(([value,unit,label])=><div key={label}><strong>{value}<small>{unit}</small></strong><span>{label}</span></div>)}</div></main>
 <aside className="inspector"><div className="right-tabs">{['inspect','configure','validation','export'].map(t=><button key={t} aria-label={t} title={t} onClick={()=>setTab(t)} className={tab===t?'active':''}>{t==='inspect'?<Info size={17}/>:t==='configure'?<SlidersHorizontal size={17}/>:t==='validation'?<TriangleAlert size={17}/>:<Download size={17}/>}</button>)}</div><div className="inspector-content"><div className="eyebrow">{tab==='inspect'?'COMPONENT INSPECTOR':tab.toUpperCase()}</div><h2>{selected==='BESS'?'System overview':selected}</h2><span className="tag">{node?.kind??'component'} · concept</span><section><h3>Electrical configuration</h3><label className="field-label">Topology preset<select aria-label="Topology preset" value={config.preset} onChange={e=>{update(c=>{c.preset=e.target.value as typeof c.preset;});focus('BESS');}}><option value="reference">Reference · 4S × 12P</option><option value="alternative">Alternative · 3S × 16P</option></select></label><p className="muted">{s.seriesPacks} packs in series per string. {s.parallelStrings} parallel strings.</p><div className="data-row"><span>Cell equivalent</span><b>{s.equivalent}</b></div><div className="data-row"><span>Operating voltage</span><b>{s.minVoltage.toLocaleString()}–{s.maxVoltage.toLocaleString()} V</b></div><div className="data-row"><span>Aggregate capacity</span><b>{s.capacity.toLocaleString()} Ah</b></div><div className="data-row"><span>Supplied string current</span><b>157 A</b></div><div className="data-row"><span>Nominal duration</span><b>{s.hours} hours</b></div></section>{tab==='configure'?<ConfigPanel model={model}/>:tab==='export'?<ExportPanel model={model} viewer={viewer}/>:tab==='validation'?<section><h3>Design review · {errors.length} errors</h3>{model.warnings.map(w=><div key={w.code} className={`notice ${w.level}`}><b>{w.code.replaceAll('-',' ')}</b><p>{w.text}</p></div>)}{config.equipment.maxVoltage!==null&&!errors.some(w=>w.code==='overvoltage')&&<div className="notice info"><b>Maximum-voltage comparison passed</b><p>Full operating-voltage and current compatibility remains subject to PCS review.</p></div>}<p className="muted">Geometry checks do not establish regulatory compliance. Routing uses defined corridors; bend radius is an unvalidated design assumption. Terminals and ancillaries are conceptual.</p></section>:<InspectPanel model={model}/>}</div></aside></div><footer><span><span className="status-dot"/> Local configuration</span><p>{qualification}</p><span>{s.minVoltage.toLocaleString()}–{s.maxVoltage.toLocaleString()} V · {errors.length} errors</span></footer>{message&&<div className="toast" role="status">{message}<button onClick={()=>notify('')} aria-label="Dismiss"><X size={16}/></button></div>}</div>;
}
function belongsSelection(id:string,parent:string){return id===parent||id.startsWith(parent+'/');}
